query Posts($locales: [Locale!]!) {
  posts(locales: $locales) {
    id
    content {
      html
    }
    coverImage(locales: [tr, en]) {
      url
    }
    date
    locale
    title
    tags
    excerpt
    createdAt
    createdBy {
      name
    }
  }
}
