query Posts($locales: [Locale!] = [tr]) {
  posts(locales: $locales) {
    id
    content {
      html
    }
    coverImage {
      url
    }
    date
    locale
    title
    tags
    excerpt
    createdAt
    createdBy {
      name
    }
  }
}
