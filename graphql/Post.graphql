query Post($id: ID!) {
  post(where: { id: $id }) {
    id
    title
    coverImage {
      url
    }
    createdAt
    createdBy {
      name
      picture
    }
    content {
      raw
    }
    tags
    excerpt
  }
}
