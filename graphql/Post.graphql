query Post($id: ID!) {
  post(where: { id: $id }) {
    title
    coverImage {
      url
    }
    createdAt
    createdBy {
      name
      picture
    }
    content {
      html
    }
    tags
    excerpt
  }
}
